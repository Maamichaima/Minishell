export p12=jjjj -->(DONE)

heredoc --> " | ' (del) --> expand

heredoc expand dima (ldakheeel)  -------------------> (done)

expand ila kan num flwel

builtins expand -------------> (done)

valid syntax (makamlach)

GARBAGE-COLLECTION

SIGNALS

export(last variable)------------->(done)

expand( space in variable example {var = "ls -al"})

!!!!!!!!!!!!!!!!!!!!
