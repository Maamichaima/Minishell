export p12=jjjj -->(DONE)

heredoc expand dima (ldakheeel)  -------------------> (done)
GARBAGE-COLLECTION (done)
export(last variable)------------->(done)

heredoc --> " | ' (del) --> expand(done)
expand ila kan num flwel(done)


builtins expand -------------> ch

valid syntax (makamlach) ch


SIGNALS  bashbash riri

expand( space in variable example {var = "ls -al"}) riri

leaks ch

heredoc 1 process (SIGNALS)

!!!!!!!!!!!!!!!!!!!!

bash: ls > $rr: ambiguous redirect
